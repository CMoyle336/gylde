# Firebase App Hosting configuration - Base/Production
runConfig:
  concurrency: 80
  cpu: 1
  memoryMiB: 512
  minInstances: 0
  maxInstances: 100

scripts:
  buildCommand: npm run build -- --configuration=production
