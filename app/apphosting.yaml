# Firebase App Hosting configuration - Base/Default (Production)
# This is used when no environment-specific yaml file matches

runConfig:
  concurrency: 80
  cpu: 1
  memoryMiB: 512
  minInstances: 0
  maxInstances: 100

scripts:
  buildCommand: ng build --configuration=production
