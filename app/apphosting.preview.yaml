# Firebase App Hosting configuration - Preview Environment
# This file is used when the backend's Environment name is set to "preview"

runConfig:
  concurrency: 80
  cpu: 1
  memoryMiB: 512
  minInstances: 0
  maxInstances: 100

scripts:
  buildCommand: ng build --configuration=preview
