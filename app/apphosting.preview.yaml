# Firebase App Hosting configuration - Preview Environment
# Used when backend's Environment name is set to "preview" in Firebase Console
runConfig:
  concurrency: 80
  cpu: 1
  memoryMiB: 512
  minInstances: 0
  maxInstances: 100

env:
  - variable: NG_BUILD_CONFIG
    value: preview
    availability:
      - BUILD
